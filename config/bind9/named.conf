// adguard-bind9 v2
// Self-contained resolver configuration.  BIND's compiled-in root hints are
// used when no explicit root hint zone is configured.

include "/etc/bind/named.conf.options";
include "/etc/bind/named.conf.local";
include "/etc/bind/named.conf.local-zones";
